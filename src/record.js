console.log("record.js")

class Record {
    constructor(recordData) {
        debugger
        this.id = recordData.id
        this.temperature = recordData.temperature
        this.pulse = recordData.pulse
        this.pain = recordData.pain
        this.comments = recordData.comments
        this.chart = recordData.chart
    }
}